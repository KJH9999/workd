<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="youtube">
	<select id="selectYoutubeList" resultType="YoutubeDTO">
		SELECT *
		FROM youtubeList
		LIMIT {start}, 5
		ORDER BY publishedAt ASC
	</select>
	
	<insert id="insertYoutubeList">
		INSERT INTO user(title, videoUrl, thumnailUrl, description, publishedAt)
		VALUES(#{title}, #{videoUrl}, #{thumnailUrl}, #{description}, #{publishedAt})
	</insert>
		
</mapper>